// Use the computer's LAN IP for real-device builds so Android can reach the backend.
export const API_BASE_URL = 'http://10.124.6.15:8000'
export const API_TIMEOUT = 2500
export const ENABLE_BACKEND_FALLBACK = false
